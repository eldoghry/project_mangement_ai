# Backend
PORT=4000
JWT_SECRET=replace-with-a-long-random-secret
NODE_ENV=production

# Frontend (baked into the Next.js build — must match where backend is reachable from the browser)
NEXT_PUBLIC_API_URL=http://localhost:4000

# AI (Phase 4)
OPENROUTER_API_KEY=